# Requirements for Autonomous Law Firm (practice-area-neutral)
# Add production dependencies here as you develop the package

requests
# Optional: For statute scraping (Justia, etc.)
# bs4  # install with: pip install beautifulsoup4
# Optional: For Google Cloud Storage backup
# google-cloud-storage  # install with: pip install google-cloud-storage

# To install all optional dependencies:
# pip install requests beautifulsoup4 google-cloud-storage
